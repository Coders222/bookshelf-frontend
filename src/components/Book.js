import React from 'react'

export default function Book(props){
    return (
        <div class={`w-36 h-12 bg-${props.color}-left flex items-center justify-center border border-solid border-1 border-black`}>Book</div>
    )
}
